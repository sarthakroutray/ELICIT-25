{
  "name": "elicit-25-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
  "start": "node server.js",
  "dev": "node server.js",
  "postinstall": "rm -rf node_modules/.cache || true",
  "build": "node -e \"console.log('No build step for server (plain Node ESM).');\""
  },
  "dependencies": {
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0",
    "mongoose": "^8.5.3",
    "nodemailer": "^7.0.5"
  }
}
